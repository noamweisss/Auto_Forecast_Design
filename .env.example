# Copy this file to .env for local development.
# Never commit real credentials. Email delivery is not implemented yet, so
# these values are not required for the current test suite or rendering work.

SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
EMAIL_ADDRESS=
EMAIL_PASSWORD=
RECIPIENT_EMAIL=
