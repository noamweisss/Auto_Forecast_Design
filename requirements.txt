# Python Dependencies for IMS Weather Forecast Generator
#
# Install all dependencies with:
#   pip install -r requirements.txt
#
# Or create a virtual environment first (recommended):
#   python -m venv venv
#   venv\Scripts\activate  (Windows)
#   pip install -r requirements.txt

# === Core Dependencies ===

# HTTP requests for fetching XML data from IMS
requests>=2.31.0

# Final PNG validation (confirms the rendered image is a valid 1080x1920 PNG).
# Rendering itself is done by the browser via Playwright, not Pillow.
Pillow>=10.0.0

# XML parsing with proper encoding support
lxml>=4.9.0

# Environment variable management (for email credentials)
python-dotenv>=1.0.0

# IANA timezone data for ZoneInfo on Windows and minimal containers
tzdata>=2025.2

# Hebrew calendar formatting for the verified Story header
pyluach>=2.3,<3


# === HTML Rendering Support ===

# Template engine for injecting forecast data into HTML design
jinja2>=3.1.0

# Headless browser automation for taking HTML-to-image screenshots
playwright>=1.40.0


# === Testing (Development Only) ===

# Test framework
pytest>=7.4.0

# Test coverage reporting
pytest-cov>=4.1.0
